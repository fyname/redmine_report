<h2><%= l(:report_title_create_daily) %></h2>
<% labelled_tabular_form_for :search, @search,
  :url => {:controller=>"search",:action => "search" },
  :html => { :class => nil, :multipart => true, :id => "search-form" } do |form| %>
  <%= render :partial => 'search/form', :locals => { :f => form } %>
  <%= submit_tag l(:report_button_daily_create) %>
<% end %>