<h2><%= l(:report_title_edit_daily) %></h2>
<% labelled_tabular_form_for :userdaily, @userdaily,
  :url => { :action => "update",:id => @userdaily.id },
  :html => { :class => nil, :multipart => true, :id => "userdaily-form" } do |form| %>
  <%= render :partial => 'form', :locals => { :f => form } %>
<p><%= submit_tag l(:report_button_edit) %> <%= link_to l(:report_link_daily_back) ,:controller=>'report',:action => 'userindex' %></p>
<% end %>